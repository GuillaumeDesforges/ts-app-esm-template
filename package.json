{
  "private": "true",
  "workspaces": [
    "shared",
    "api",
    "client"
  ],
  "scripts": {
    "build": "tsc --build --force ./tsconfig.json",
    "build:watch": "tsc --build --force ./tsconfig.json --watch",
    "client:dev": "yarn workspace client webpack serve --open"
  },
  "dependencies": {
    "remult": "^0.16.1"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "typescript": "^4.8.3"
  }
}
