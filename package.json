{
  "private": "true",
  "workspaces": [
    "shared",
    "api"
  ],
  "scripts": {
    "build": "tsc --build --force ./tsconfig.json",
    "build:watch": "tsc --build --force ./tsconfig.json --watch"
  },
  "dependencies": {
    "remult": "^0.16.1"
  },
  "devDependencies": {
    "typescript": "^4.8.3"
  }
}
